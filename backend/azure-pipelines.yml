trigger:
  branches:
    include:
      - main
  paths:
    exclude:
      - README.md
      - docs/*

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: roger-mdc-backend-vars
  - name: GO_VERSION
    value: '1.21'
  - name: BINARY_NAME
    value: 'api'

stages:
  # ============================================
  # STAGE 1: Build and Test
  # ============================================
  - stage: Build
    displayName: 'Build & Test'
    jobs:
      - job: BuildJob
        displayName: 'Build Go Application'
        steps:
          # Install Go
          - task: GoTool@0
            displayName: 'Install Go $(GO_VERSION)'
            inputs:
              version: '$(GO_VERSION)'

          # Download dependencies
          - script: |
              go mod download
              go mod verify
            displayName: 'Download dependencies'
            workingDirectory: '$(Build.SourcesDirectory)'

          # Run linting
          - script: |
              go vet ./...
            displayName: 'Go vet (linting)'
            workingDirectory: '$(Build.SourcesDirectory)'

          # Run tests - FAIL BUILD ON TEST FAILURES
          - script: |
              go test -v -race -coverprofile=coverage.out ./...
            displayName: 'Run tests'
            workingDirectory: '$(Build.SourcesDirectory)'

          # Check test coverage (informational, doesn't fail build)
          - script: |
              go tool cover -func=coverage.out | tail -1
            displayName: 'Report test coverage'
            workingDirectory: '$(Build.SourcesDirectory)'
            continueOnError: true

          # Build the application
          - script: |
              CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o $(Build.ArtifactStagingDirectory)/$(BINARY_NAME) ./cmd/api
            displayName: 'Build binary'
            workingDirectory: '$(Build.SourcesDirectory)'

          # Security: Check for vulnerabilities in dependencies
          - script: |
              go install golang.org/x/vuln/cmd/govulncheck@latest
              govulncheck ./... || true
            displayName: 'Security vulnerability scan'
            workingDirectory: '$(Build.SourcesDirectory)'
            continueOnError: true

          # Copy necessary files including migrations
          - script: |
              cp Dockerfile $(Build.ArtifactStagingDirectory)/
              cp -r prometheus.yml $(Build.ArtifactStagingDirectory)/ 2>/dev/null || true
              cp -r migrations $(Build.ArtifactStagingDirectory)/ 2>/dev/null || true
            displayName: 'Copy deployment files'
            workingDirectory: '$(Build.SourcesDirectory)'

          # Publish artifact
          - task: PublishBuildArtifacts@1
            displayName: 'Publish artifact'
            inputs:
              PathtoPublish: '$(Build.ArtifactStagingDirectory)'
              ArtifactName: 'backend-drop'
              publishLocation: 'Container'

  # ============================================
  # STAGE 2: Deploy to Azure App Service
  # ============================================
  - stage: Deploy
    displayName: 'Deploy to Azure'
    dependsOn: Build
    condition: succeeded()
    jobs:
      - deployment: DeployJob
        displayName: 'Deploy to App Service'
        environment: 'production'
        strategy:
          runOnce:
            deploy:
              steps:
                # Download artifact
                - download: current
                  artifact: backend-drop

                # Run database migrations before deployment
                - script: |
                    echo "Running database migrations..."
                    # Install golang-migrate
                    curl -L https://github.com/golang-migrate/migrate/releases/download/v4.17.0/migrate.linux-amd64.tar.gz | tar xvz
                    ./migrate -path $(Pipeline.Workspace)/backend-drop/migrations -database "$(DB_URL)" up
                  displayName: 'Run database migrations'
                  env:
                    DB_URL: $(DATABASE_URL)
                  condition: and(succeeded(), ne(variables['DATABASE_URL'], ''))

                # Deploy to Azure App Service
                - task: AzureWebApp@1
                  displayName: 'Deploy to Azure App Service'
                  inputs:
                    azureSubscription: '$(AZURE_SERVICE_CONNECTION)'
                    appType: 'webAppLinux'
                    appName: '$(AZURE_WEBAPP_NAME)'
                    package: '$(Pipeline.Workspace)/backend-drop/$(BINARY_NAME)'
                    runtimeStack: 'GO|1.21'

                # Health check
                - script: |
                    echo "Waiting for deployment to stabilize..."
                    sleep 30
                    curl -f https://$(AZURE_WEBAPP_NAME).azurewebsites.net/health || echo "Health check endpoint not available"
                  displayName: 'Health check'
                  continueOnError: true
